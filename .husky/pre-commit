echo "🔍 Running pre-commit checks..."

echo "🔒 Founder privacy check..."
npm run check:founder-privacy || exit 1

echo "🔒 Demo privacy check..."
npm run check:demo-privacy || exit 1

# Skip linter for now (ESLint v9 config needed)
# echo "📝 Running linter..."
# cd apps/web && npm run lint || echo "⚠️  Linter has warnings"

# Run critical tests (fast tests only)
echo "🧪 Running critical tests..."
cd apps/server
if npm test -- --run 2>/dev/null; then
  echo "✅ Server tests passed"
else
  echo "⚠️  Server tests failed. Consider fixing before committing."
fi

cd ../web
if npm run test:unit -- --run 2>/dev/null; then
  echo "✅ Web unit tests passed"
else
  echo "⚠️  Web unit tests failed. Consider fixing before committing."
fi

echo "✅ Pre-commit checks completed!"
