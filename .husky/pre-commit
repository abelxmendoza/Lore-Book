echo "🔍 Running pre-commit checks..."

echo "🔒 Founder privacy check..."
npm run check:founder-privacy || exit 1

echo "🔒 Demo privacy check..."
npm run check:demo-privacy || exit 1

# Skip linter for now (ESLint v9 config needed)
# echo "📝 Running linter..."
# cd apps/web && npm run lint || echo "⚠️  Linter has warnings"

# Full test suites — blocking. (Previously non-blocking: a failure only
# printed a warning and let the commit through, which is how a real
# regression reached main undetected in an earlier commit today.)
echo "🧪 Running server tests..."
(cd apps/server && npm test -- --run) || {
  echo "❌ Server tests failed. Fix before committing (or use --no-verify only if you understand the risk)."
  exit 1
}
echo "✅ Server tests passed"

echo "🧪 Running web tests..."
(cd apps/web && npm run test:unit -- --run) || {
  echo "❌ Web tests failed. Fix before committing (or use --no-verify only if you understand the risk)."
  exit 1
}
echo "✅ Web tests passed"

echo "✅ Pre-commit checks completed!"
