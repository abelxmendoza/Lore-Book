name: Legacy Full Test Suites

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  NODE_VERSION: '20'
  HUSKY: '0'

jobs:
  note:
    name: Manual-Only Notice
    runs-on: ubuntu-latest
    steps:
      - name: Explain workflow status
        run: |
          echo "The required pull request checks live in .github/workflows/ci.yml."
          echo "This legacy workflow is kept manual while full lint/unit/e2e debt is paid down."
