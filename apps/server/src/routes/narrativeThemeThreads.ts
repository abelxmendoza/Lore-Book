/**
 * Canonical mount for saga/arc theme threads (timeline hierarchy memberships).
 * Legacy /api/threads remains active — same router, clearer namespace.
 */
export { threadsRouter as narrativeThemeThreadsRouter } from './threads';
