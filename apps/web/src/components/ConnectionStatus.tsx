/**
 * Legacy connection toast — superseded by BackendUnavailableBanner.
 * Kept as a no-op export so imports don't break; avoids duplicate red error cards.
 */
export const ConnectionStatus = () => null;
