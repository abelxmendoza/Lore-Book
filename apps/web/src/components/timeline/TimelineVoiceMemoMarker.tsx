export const TimelineVoiceMemoMarker = ({ count }: { count: number }) => (
  <div className="rounded-lg border border-primary/30 bg-primary/10 px-4 py-3 text-sm text-primary">
    Voice memos synced: {count}
  </div>
);
