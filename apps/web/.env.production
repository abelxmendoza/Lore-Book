# Supabase — primary custom domain; fallback if add-on cancelled:
# VITE_SUPABASE_URL=https://supabase.lorebookai.com
# VITE_SUPABASE_URL=https://cshtthzpgkmrbcsfghyq.supabase.co
#
# Production API routing:
# - Vercel deploys use same-origin /api/* proxy (see vercel.json rewrites) with VITE_API_URL=""
# - For local production preview against Railway directly, uncomment the line below:
# VITE_API_URL=https://lore-book-production.up.railway.app
VITE_API_URL=
# VITE_OWNER_EMAIL / VITE_DEVELOPER_EMAIL / VITE_ADMIN_EMAIL — set in Vercel dashboard only
