{
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.4 --activate && pnpm --filter=web --workspace-root install",
  "buildCommand": "pnpm --filter=web build",
  "outputDirectory": "apps/web/dist"
}

